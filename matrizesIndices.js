/*Acessar dados de matriz com índices
Podemos acessar os dados dentro de matrizes usando índices .

Índices de array são escritos na mesma notação de colchetes que strings usam, exceto que em vez de especificar um caractere, eles estão especificando uma entrada no array. Assim como strings, arrays usam indexação baseada em zero , então o primeiro elemento em um array tem um índice de 0.


Exemplo

const array = [50, 60, 70];
console.log(array[0]);
const data = array[1];
As console.log(array[0])impressões 50, e datatem o valor 60.

Crie uma variável chamada myDatae defina-a para ser igual ao primeiro valor de myArrayusando a notação de colchetes.*/

const myArray = [50, 60, 70];

const myData = myArray[0];

