/*Declarar Variáveis ​​JavaScript
Em ciência da computação, dados são tudo o que é significativo para o computador. JavaScript fornece oito tipos de dados diferentes que são undefined, null, boolean, string, symbol, bigint, number, e object.

Por exemplo, os computadores distinguem entre números, como o número 12, e strings, como "12", "dog", ou "123 cats", que são coleções de caracteres. Os computadores podem executar operações matemáticas em um número, mas não em uma string.

Variáveis ​​permitem que computadores armazenem e manipulem dados de forma dinâmica. Eles fazem isso usando um "rótulo" para apontar para os dados em vez de usar os dados em si. Qualquer um dos oito tipos de dados pode ser armazenado em uma variável.

Variáveis ​​são similares às variáveis ​​x e y que você usa em matemática, o que significa que elas são um nome simples para representar os dados aos quais queremos nos referir. Variáveis ​​de computador diferem de variáveis ​​matemáticas porque podem armazenar valores diferentes em momentos diferentes.

Dizemos ao JavaScript para criar ou declarar uma variável colocando a palavra-chave varna frente dela, assim:

var ourName;
cria uma variável chamada ourName. Em JavaScript, terminamos as instruções com ponto e vírgula. Nomes de variáveis ​​podem ser compostos de números, letras e $ou _, mas não podem conter espaços ou começar com um número.

Use a varpalavra-chave para criar uma variável chamada myName.

Dica:
Veja o ourNameexemplo acima se você ficar preso. */

var myName;