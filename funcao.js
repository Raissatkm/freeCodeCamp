/*Escreva JavaScript reutilizável com funções
Em JavaScript, podemos dividir nosso código em partes reutilizáveis ​​chamadas funções .

Aqui está um exemplo de uma função:

function functionName() {
  console.log("Hello World");
}
Você pode chamar ou invocar essa função usando seu nome seguido por parênteses, assim: functionName();Toda vez que a função for chamada, ela imprimirá a mensagem Hello Worldno console dev. Todo o código entre as chaves será executado toda vez que a função for chamada.

Crie uma função chamada reusableFunctionque imprime a string Hi Worldno console de desenvolvimento.
Chame a função. */

function reusableFunction(){
    console.log("Hi World")
  }
  
  reusableFunction();